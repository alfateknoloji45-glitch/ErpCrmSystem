<UserControl x:Class="ErpCrm.Desktop.Views.FaturaListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <!-- Toolbar -->
        <Border Grid.Row="0" 
                Background="{StaticResource BackgroundBrush}"
                Padding="15"
                CornerRadius="8,8,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <!-- Başlık -->
                <TextBlock Grid.Column="0"
                           Text="📄 Faturalar"
                           FontSize="18"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource PrimaryBrush}" />
                
                <!-- Butonlar -->
                <StackPanel Grid.Column="1" 
                            Orientation="Horizontal">
                    <Button Content="➕ Yeni Fatura"
                            Command="{Binding NewCommand}"
                            Style="{StaticResource PrimaryButton}"
                            Margin="0,0,10,0" />
                    
                    <Button Content="👁️ Görüntüle"
                            Command="{Binding ViewCommand}"
                            Background="#FFC107"
                            Foreground="#333"
                            BorderThickness="0"
                            Padding="15,10"
                            Cursor="Hand"
                            Margin="0,0,10,0" />
                    
                    <Button Content="🖨️ Yazdır"
                            Command="{Binding PrintCommand}"
                            Background="#607D8B"
                            Foreground="White"
                            BorderThickness="0"
                            Padding="15,10"
                            Cursor="Hand"
                            Margin="0,0,10,0" />
                    
                    <Button Content="🔄 Yenile"
                            Command="{Binding RefreshCommand}"
                            Background="#607D8B"
                            Foreground="White"
                            BorderThickness="0"
                            Padding="15,10"
                            Cursor="Hand" />
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- DataGrid -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Faturalar}"
                  SelectedItem="{Binding SelectedFatura}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  BorderThickness="0"
                  RowHeight="40"
                  ColumnHeaderHeight="45"
                  Background="White"
                  AlternatingRowBackground="#FAFAFA">
            
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="Padding" Value="15,10" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                </Style>
            </DataGrid.ColumnHeaderStyle>
            
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="15,10" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Padding="{TemplateBinding Padding}" 
                                        Background="{TemplateBinding Background}">
                                    <ContentPresenter VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#E3F2FD" />
                            <Setter Property="Foreground" Value="Black" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fatura No" 
                                    Binding="{Binding FaturaNo}" 
                                    Width="150" />
                
                <DataGridTextColumn Header="Tarih" 
                                    Binding="{Binding FaturaTarihi, StringFormat='{}{0:dd.MM.yyyy}'}" 
                                    Width="120" />
                
                <DataGridTextColumn Header="Tip" 
                                    Binding="{Binding FaturaTipiText}" 
                                    Width="100" />
                
                <DataGridTextColumn Header="Müşteri/Tedarikçi" 
                                    Binding="{Binding CariAdi}" 
                                    Width="*" />
                
                <DataGridTextColumn Header="Ara Toplam" 
                                    Binding="{Binding AraToplam, StringFormat='{}{0:N2} ₺'}" 
                                    Width="130">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                
                <DataGridTextColumn Header="KDV" 
                                    Binding="{Binding KdvToplam, StringFormat='{}{0:N2} ₺'}" 
                                    Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                
                <DataGridTextColumn Header="Genel Toplam" 
                                    Binding="{Binding GenelToplam, StringFormat='{}{0:N2} ₺'}" 
                                    Width="140">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right" />
                            <Setter Property="FontWeight" Value="Bold" />
                            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                
                <DataGridTextColumn Header="Durum" 
                                    Binding="{Binding DurumText}" 
                                    Width="100" />
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- Alt Bar (Durum) -->
        <Border Grid.Row="2" 
                Background="{StaticResource BackgroundBrush}"
                Padding="15"
                CornerRadius="0,0,8,8">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Toplam: " 
                               Foreground="Gray" />
                    <TextBlock Text="{Binding Faturalar.Count}" 
                               FontWeight="SemiBold" />
                    <TextBlock Text=" fatura" 
                               Foreground="Gray" />
                </StackPanel>
                
                <!-- Yükleme Göstergesi -->
                <TextBlock Text="Yükleniyor..." 
                           HorizontalAlignment="Right"
                           Foreground="{StaticResource PrimaryBrush}"
                           Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}" />
                
                <!-- Hata Mesajı -->
                <TextBlock Text="{Binding ErrorMessage}" 
                           HorizontalAlignment="Right"
                           Foreground="{StaticResource ErrorBrush}"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
