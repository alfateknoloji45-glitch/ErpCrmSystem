<Window x:Class="ErpCrm.Desktop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewmodels="clr-namespace:ErpCrm.Desktop.ViewModels"
        xmlns:views="clr-namespace:ErpCrm.Desktop.Views"
        Title="ERP/CRM/POS Sistemi" 
        Height="700" Width="1100"
        WindowStartupLocation="CenterScreen"
        MinHeight="600" MinWidth="900"
        Background="{StaticResource BackgroundBrush}">
    
    <Window.DataContext>
        <viewmodels:MainViewModel />
    </Window.DataContext>
    
    <Window.Resources>
        <!-- DataTemplate for CariListViewModel -->
        <DataTemplate DataType="{x:Type viewmodels:CariListViewModel}">
            <views:CariListView />
        </DataTemplate>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <!-- Sol Menü -->
        <Border Grid.Column="0" 
                Background="{StaticResource SidebarBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                
                <!-- Firma Bilgisi -->
                <Border Grid.Row="0" 
                        Padding="16" 
                        BorderBrush="#37474F" 
                        BorderThickness="0,0,0,1">
                    <StackPanel>
                        <TextBlock Text="{Binding FirmaAdi}" 
                                   FontSize="16" 
                                   FontWeight="Bold"
                                   Foreground="{StaticResource SidebarTextBrush}" />
                        
                        <TextBlock Text="{Binding KullaniciAdi}" 
                                   FontSize="12" 
                                   Foreground="#90A4AE" 
                                   Margin="0,5,0,0" />
                        
                        <!-- Demo Bilgisi -->
                        <Border Background="{StaticResource WarningBrush}" 
                                CornerRadius="4" 
                                Padding="8,4" 
                                Margin="0,10,0,0"
                                Visibility="{Binding DemoMu, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock Text="{Binding DemoBilgisi}" 
                                       FontSize="11" 
                                       Foreground="#333" 
                                       FontWeight="SemiBold" />
                        </Border>
                    </StackPanel>
                </Border>
                
                <!-- Menü Öğeleri -->
                <ScrollViewer Grid.Row="1" 
                              VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding MenuItems}" 
                                  Margin="0,10">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Title}"
                                        Command="{Binding Command}"
                                        Style="{StaticResource MenuItemButton}"
                                        HorizontalAlignment="Stretch" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
                
                <!-- Çıkış -->
                <Border Grid.Row="2" 
                        Padding="16" 
                        BorderBrush="#37474F" 
                        BorderThickness="0,1,0,0">
                    <Button Content="🚪 Çıkış Yap"
                            Command="{Binding LogoutCommand}"
                            Style="{StaticResource MenuItemButton}"
                            HorizontalAlignment="Stretch" />
                </Border>
            </Grid>
        </Border>
        
        <!-- Üst Bar + İçerik -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="60" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            
            <!-- Üst Bar -->
            <Border Grid.Row="0" 
                    Background="White"
                    BorderBrush="#E0E0E0"
                    BorderThickness="0,0,0,1">
                <Grid Margin="20,0">
                    <TextBlock Text="ERP/CRM/POS Sistemi" 
                               FontSize="18" 
                               FontWeight="SemiBold"
                               Foreground="{StaticResource PrimaryBrush}"
                               VerticalAlignment="Center" />
                    
                    <StackPanel Orientation="Horizontal" 
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center">
                        <TextBlock Text="📅 " 
                                   FontSize="14" 
                                   VerticalAlignment="Center" />
                        <TextBlock Text="{Binding Source={x:Static System:DateTime.Today}, StringFormat='{}{0:dd MMMM yyyy}'}" 
                                   xmlns:System="clr-namespace:System;assembly=mscorlib"
                                   FontSize="14" 
                                   Foreground="Gray"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- İçerik Alanı -->
            <Border Grid.Row="1" 
                    Margin="20">
                <!-- Dinamik içerik veya Hoşgeldiniz ekranı -->
                <ContentControl Content="{Binding CurrentViewModel}">
                    <ContentControl.Style>
                        <Style TargetType="ContentControl">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentViewModel}" Value="{x:Null}">
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <!-- Hoşgeldiniz Ekranı -->
                                            <Border Background="White" 
                                                    CornerRadius="8"
                                                    Padding="40">
                                                <StackPanel VerticalAlignment="Center" 
                                                            HorizontalAlignment="Center">
                                                    <TextBlock Text="🏢" 
                                                               FontSize="72" 
                                                               HorizontalAlignment="Center" />
                                                    
                                                    <TextBlock Text="Hoş Geldiniz!" 
                                                               FontSize="28" 
                                                               FontWeight="Bold"
                                                               HorizontalAlignment="Center"
                                                               Foreground="{StaticResource PrimaryBrush}"
                                                               Margin="0,20,0,10" />
                                                    
                                                    <TextBlock Text="Sol menüden bir modül seçerek başlayabilirsiniz." 
                                                               FontSize="14" 
                                                               Foreground="Gray"
                                                               HorizontalAlignment="Center" />
                                                    
                                                    <Border Background="{StaticResource BackgroundBrush}" 
                                                            CornerRadius="8" 
                                                            Padding="20" 
                                                            Margin="0,30,0,0">
                                                        <StackPanel>
                                                            <TextBlock Text="Hızlı İpuçları:" 
                                                                       FontWeight="SemiBold" 
                                                                       Margin="0,0,0,10" />
                                                            
                                                            <TextBlock Text="• 👥 Cari Yönetimi: Müşteri ve tedarikçi bilgilerinizi yönetin"
                                                                       FontSize="12" 
                                                                       Margin="0,5" />
                                                            
                                                            <TextBlock Text="• 📦 Stok Yönetimi: Ürünlerinizi ve stoklarınızı takip edin"
                                                                       FontSize="12" 
                                                                       Margin="0,5" />
                                                            
                                                            <TextBlock Text="• 📄 Faturalar: Alış ve satış faturalarınızı oluşturun"
                                                                       FontSize="12" 
                                                                       Margin="0,5" />
                                                            
                                                            <TextBlock Text="• 🍽️ POS: Restoran/cafe satış noktası sistemi"
                                                                       FontSize="12" 
                                                                       Margin="0,5" />
                                                        </StackPanel>
                                                    </Border>
                                                </StackPanel>
                                            </Border>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ContentControl.Style>
                </ContentControl>
            </Border>
        </Grid>
    </Grid>
</Window>
