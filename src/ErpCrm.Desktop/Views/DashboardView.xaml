<UserControl x:Class="ErpCrm.Desktop.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewmodels="clr-namespace:ErpCrm.Desktop.ViewModels">
    
    <UserControl.DataContext>
        <viewmodels:DashboardViewModel />
    </UserControl.DataContext>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Text="📊 Ana Sayfa" 
                   FontSize="28" 
                   FontWeight="Bold" 
                   Foreground="#2196F3"
                   Margin="0,0,0,30"/>
        
        <!-- Stats Cards -->
        <UniformGrid Grid.Row="1" Rows="1" Columns="3" HorizontalAlignment="Stretch" VerticalAlignment="Top">
            
            <!-- Customers Card -->
            <Border Background="#E3F2FD" CornerRadius="12" Padding="30" Margin="0,0,15,0">
                <StackPanel>
                    <TextBlock Text="👥 Toplam Müşteri" FontSize="16" Foreground="#1976D2" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding ToplamMusteri}" FontSize="48" FontWeight="Bold" Foreground="#0D47A1" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
            
            <!-- Products Card -->
            <Border Background="#F3E5F5" CornerRadius="12" Padding="30" Margin="7.5,0,7.5,0">
                <StackPanel>
                    <TextBlock Text="📦 Toplam Ürün" FontSize="16" Foreground="#7B1FA2" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding ToplamUrun}" FontSize="48" FontWeight="Bold" Foreground="#4A148C" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
            
            <!-- Invoices Card -->
            <Border Background="#E8F5E9" CornerRadius="12" Padding="30" Margin="15,0,0,0">
                <StackPanel>
                    <TextBlock Text="📄 Toplam Fatura" FontSize="16" Foreground="#388E3C" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding ToplamFatura}" FontSize="48" FontWeight="Bold" Foreground="#1B5E20" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
            
        </UniformGrid>
        
        <!-- Loading Indicator -->
        <StackPanel Grid.Row="1" 
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Center"
                    Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="Yükleniyor..." 
                       FontSize="16" 
                       Foreground="Gray"/>
        </StackPanel>
    </Grid>
</UserControl>
